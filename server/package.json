{
  "name": "vchat-server",
  "version": "1.0.0",
  "description": "",
  "main": "bin/www",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node dist/server.js",
    "watch": "cross-env NODE_ENV=developmen nodemon --watch 'src/**/*' -e ts --exec 'ts-node' ./src/server.ts"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "koa": "^2.7.0",
    "koa-router": "^7.4.0",
    "typescript": "^3.3.3"
  },
  "devDependencies": {
    "@types/koa": "^2.0.48",
    "@types/koa-router": "^7.0.39",
    "nodemon": "^1.18.10",
    "ts-node": "^8.0.2"
  }
}
